[project]
name = "sans-index-builder"
version = "0.1.0"
description = "CLI tool for building SANS exam study indexes"
requires-python = ">=3.8"
dependencies = [
    "reportlab>=4.0.0",
]

[project.scripts]
sans-index = "sans_index:main"
